import React from 'react';
import {Wrapper, Animation} from './Loader.style';

const Loader = () => {
    return (
        <Wrapper>
            <Animation />
        </Wrapper>
    );
};

export default Loader;